---
layout: layouts/base/index.njk
templateClass: topic
date: Last Modified
eleventyComputed:
  title: '{{ topic | formatTopic }}'
permalink: /topics/{{ topic | slug }}/
pagination:
  data: collections
  size: 1
  filter:
    - entries
    - series
  alias: topic
---

{% import "../../_includes/shared/macros.njk" as macros %}

<section class="topic__content">
  {{ macros.breadcrumbs('Topics', topic, 'topic__breadcrumbs') }}

  <ul>
    {% for item in collections[topic] %}
      <li>
        {{ macros.contentType(item.data.tags) }}
        <a href="{{ item.url }}">
          {{ item.data.title }}
        </a>
      </li>
    {% endfor %}
  </ul>
</section>
